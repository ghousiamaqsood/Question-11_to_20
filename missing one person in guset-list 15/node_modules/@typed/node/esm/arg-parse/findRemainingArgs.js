import { get } from '@typed/effects';
export function* findRemainingArgs(usedIndexes) {
    const { args } = yield* get();
    const remainingArgs = args.filter((value, index) => value !== '--' && !usedIndexes.includes(index));
    return { _: remainingArgs };
}
//# sourceMappingURL=findRemainingArgs.js.map