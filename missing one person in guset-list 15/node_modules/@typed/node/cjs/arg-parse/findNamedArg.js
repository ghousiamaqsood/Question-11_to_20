"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.findNamedArg = void 0;
const list_1 = require("@typed/list");
const maybe_1 = require("@typed/maybe");
function findNamedArg(name, aliases, args) {
    const value = `--${name}`;
    const namedValue = list_1.findLastIndex((arg) => arg.startsWith(value), args);
    const aliasValues = aliases.map((alias) => `-${alias}`);
    return list_1.reduce((maybe, value) => maybe_1.race(maybe, list_1.findLastIndex((arg) => arg.startsWith(value), args)), namedValue, aliasValues);
}
exports.findNamedArg = findNamedArg;
//# sourceMappingURL=findNamedArg.js.map