import { Effect, get } from '@typed/effects';
export function* time(label) {
    const { logger } = yield* get();
    yield logger.timeStart(label);
    return Effect.fromEnv(logger.timeEnd(label));
}
//# sourceMappingURL=time.js.map