import { Clock } from '@typed/timer';
import { Logger, LogLevel } from './types';
export declare type CreateConsoleLoggerOptions = {
    readonly logLevel: LogLevel;
    readonly clock: Clock;
};
/**
 * Create a logger that wraps the console.
 */
export declare function createConsoleLogger({ logLevel, clock }: CreateConsoleLoggerOptions): Logger;
//# sourceMappingURL=createConsoleLogger.d.ts.map