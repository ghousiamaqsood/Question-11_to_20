import { curry } from '@typed/lambda';
/**
 * Split a string
 */
export const split = curry(__split);
function __split(separator, str) {
    return str.split(separator);
}
//# sourceMappingURL=split.js.map