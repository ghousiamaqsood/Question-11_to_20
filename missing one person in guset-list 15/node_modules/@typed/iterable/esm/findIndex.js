import { curry } from '@typed/lambda';
import { Just, Nothing } from '@typed/maybe';
export const findIndex = curry(__findIndex);
function __findIndex(predicate, iterable) {
    let i = 0;
    for (const value of iterable) {
        if (predicate(value)) {
            return Just.of(i);
        }
        i++;
    }
    return Nothing;
}
//# sourceMappingURL=findIndex.js.map