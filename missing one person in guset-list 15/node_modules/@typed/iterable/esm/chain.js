import { curry } from '@typed/lambda';
export const chain = curry(__chain);
export function* __chain(fn, iterable) {
    for (const a of iterable) {
        yield* fn(a);
    }
}
//# sourceMappingURL=chain.js.map