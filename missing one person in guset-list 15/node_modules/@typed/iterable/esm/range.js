import { curry } from '@typed/lambda';
/**
 * Inclusive range by 1
 */
export const range = curry((from, to) => __rangeBy(from, to, 1));
/**
 * Inclusive range by given increment
 */
export const rangeBy = curry(__rangeBy);
function* __rangeBy(from, to, by) {
    while (from <= to) {
        yield from;
        from += by;
    }
}
//# sourceMappingURL=range.js.map