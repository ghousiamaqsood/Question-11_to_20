import { curry } from '@typed/lambda';
export const filter = curry(__filter);
function* __filter(predicate, iterable) {
    for (const x of iterable) {
        if (predicate(x)) {
            yield x;
        }
    }
}
//# sourceMappingURL=filter.js.map