import { curry } from '@typed/lambda';
export const drop = curry(__drop);
function* __drop(amount, iterable) {
    for (const x of iterable) {
        if (--amount < 0) {
            yield x;
        }
    }
}
//# sourceMappingURL=drop.js.map