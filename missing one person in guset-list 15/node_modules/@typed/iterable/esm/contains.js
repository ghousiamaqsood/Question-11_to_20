import { curry } from '@typed/lambda';
import { equals } from '@typed/logic';
export const contains = curry(__contains);
function __contains(value, iterable) {
    for (const x of iterable) {
        if (equals(x, value)) {
            return true;
        }
    }
    return false;
}
//# sourceMappingURL=contains.js.map