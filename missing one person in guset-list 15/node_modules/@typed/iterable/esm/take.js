import { curry } from '@typed/lambda';
export const take = curry(__take);
function* __take(amount, iterable) {
    for (const x of iterable) {
        if (amount-- > 0) {
            yield x;
        }
    }
}
//# sourceMappingURL=take.js.map