import { curry } from '@typed/lambda';
export const forEach = curry(__forEach);
export function __forEach(fn, iterable) {
    for (const x of iterable) {
        fn(x);
    }
}
//# sourceMappingURL=forEach.js.map