import { curry } from '@typed/lambda';
import { chain } from './chain';
import { map } from './map';
/**
 * Apply an async iterable of functions to an async iterable of values.
 */
export const ap = curry((fn, value) => chain((f) => map(f, value), fn));
//# sourceMappingURL=ap.js.map