import { curry } from '@typed/lambda';
import { chain } from './chain';
import { Future } from './Future';
export const map = curry(__map);
function __map(fn, future) {
    return chain((b) => Future.of(fn(b)), future);
}
//# sourceMappingURL=map.js.map