import { chain } from './chain';
import { Future } from './Future';
import { map } from './map';
export function sequence(futures) {
    return futures.reduce((acc, f) => chain((bs) => map((b) => bs.concat(b), f), acc), Future.of([]));
}
//# sourceMappingURL=sequence.js.map